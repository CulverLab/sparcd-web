'use client'

/** @module components/UserActions */

// Definitions of user actions that can be globally referenced
const UserActions = {
  None: 0,
  Upload: 10,
  UploadEdit: 20,
  Collection: 30,
  Query: 40,
  Maps: 50,
  AdminEdit: 60,
};

export default UserActions;
